{{-- Colonne image : alignée en haut avec la carte formulaire (≥ xl) --}}
<div class="col-xl-3 d-none d-xl-block alliance-engagement-visual-col">
    <div class="alliance-engagement-visual">
        <img src="{{ $src }}" alt="{{ $alt ?? '' }}">
    </div>
</div>
